# Release Drafter 配置
# https://github.com/release-drafter/release-drafter

categories:
  - title: ✨ New Features
    labels:
      - feat
      - enhancement
  - title: 🐛 Bug Fixes
    labels:
      - fix
      - bugfix
  - title: ♻️ Refactoring
    labels:
      - refactor
  - title: 📝 Documentation
    labels:
      - docs
      - documentation
  - title: 🧪 Tests
    labels:
      - test
      - testing
  - title: 🔧 Chore
    labels:
      - chore
  - title: ⚙️ CI/CD
    labels:
      - ci
      - build

exclude-labels:
  - skip-release
  - release

change-title-regex: "^(\\w+)\\s+(\\S+)\\s+\\b(.+)\\b"

version-resolver:
  major:
    labels:
      - breaking
  minor:
    labels:
      - feat
      - enhancement
  patch:
    labels:
      - fix
      - bugfix
      - refactor
      - chore
      - test
      - docs
      - ci

template: |
  ## What's Changed

  $CHANGES

  ### 下载
  - `windows-folder-remark-${{VERSION}}.exe`: 单文件可执行程序，无需安装 Python
  - `.sha256` 文件用于验证下载文件的完整性

  ### 使用方法
  ``` powershell
  # 添加备注
  .\windows-folder-remark-${{VERSION}}.exe "C:\MyFolder" "我的备注"

  # 查看备注
  .\windows-folder-remark-${{VERSION}}.exe --view "C:\MyFolder"

  # 删除备注
  .\windows-folder-remark-${{VERSION}}.exe --delete "C:\MyFolder"

  # 交互模式
  .\windows-folder-remark-${{VERSION}}.exe
  ```

  ### 验证下载
  ``` powershell
  certutil -hashfile windows-folder-remark-${{VERSION}}.exe SHA256
  ```
  然后对比生成的哈希值与 .sha256 文件中的值是否一致。

  ### 系统要求
  - Windows 7 或更高版本
