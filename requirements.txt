# Windows Folder/File Remark Tool
#
# Dependencies for Windows folder and file comment management tool.
#
# Install: pip install -r requirements.txt

# pywin32 - Windows API and COM support
# Required for:
#   - Windows Property Store API (IPropertyStore, SHGetPropertyStoreFromParsingName)
#   - Shell extensions (SHChangeNotify)
#   - COM component registration (for Shell Column Handler)
#   - pythoncom (COM runtime)
#   - win32com.server.register (COM server registration)
#   - win32con, win32api (Windows constants and API)
#   - win32com.shell (Shell interfaces)
pywin32>=305